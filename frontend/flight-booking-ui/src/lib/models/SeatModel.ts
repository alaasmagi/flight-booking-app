export interface SeatModel {
    id: number,
    extraFee: string;
    extraLegroom: string;
    nearExit: string;
    window: string;
    rownNumber: string;
    seatClass: string;
    seatLetter: number;
}